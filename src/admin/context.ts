// import { BehaviorSubject } from 'rxjs'
// import { User } from './users/users.api'
// import { createContext } from '@lit/context'

// export const DevicesContext = createContext<Map<string, IDevice>>(undefined)

// export const $devices = new BehaviorSubject<Map<string, IDevice>>(
// 	new Map(Object.keys(devicesData).map(id => [id, devicesData[id]])),
// )
